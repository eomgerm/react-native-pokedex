import { useRoute } from "@react-navigation/native";
import { Text } from "react-native";
import Pokemon from "../../../types/pokemon";

const PokemonDetail = () => {
  /*
  const route = useRoute();
  const {
    params: { pokemon },
  } = route;
  console.log(pokemon);
  */
  return <Text>PokemonDetail</Text>;
};

export default PokemonDetail;
