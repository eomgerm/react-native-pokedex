import { Text, View } from "react-native";
import { TabProps } from "../Details";

const Moves = ({ pokemon }: TabProps) => {
  return (
    <View>
      <Text>Moves</Text>
    </View>
  );
};

export default Moves;
